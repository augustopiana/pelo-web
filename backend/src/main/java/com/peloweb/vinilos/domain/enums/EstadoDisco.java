package com.peloweb.vinilos.domain.enums;

/** Estado general del disco segun la escala Goldmine (spec 4.2, D-1). */
public enum EstadoDisco {
    MINT,
    NEAR_MINT,
    VG_PLUS_PLUS,
    VG_PLUS,
    VG,
    GOOD,
    POOR
}
