# URL del backend en dev. La consume src/lib/api.ts via import.meta.env.
VITE_API_URL=http://localhost:8080

# Client ID de Google (login con Google). Vacío = botón deshabilitado.
# Debe coincidir con GOOGLE_CLIENT_ID del backend. Se carga en .env.local (no versionado).
VITE_GOOGLE_CLIENT_ID=
