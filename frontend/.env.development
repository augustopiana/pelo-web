# URL del backend en dev. La consume src/lib/api.ts via import.meta.env.
VITE_API_URL=http://localhost:8080

# Nota: VITE_GOOGLE_CLIENT_ID NO se define acá (este archivo tiene prioridad
# sobre .env.local en modo dev). El Client ID va en .env.local (no versionado).
# Ver .env.example.
